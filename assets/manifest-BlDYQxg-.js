const version = "2026.04.a-seed";
const engine = "none";
const renderedAt = null;
const notes = "Empty seed manifest. Populate by running `npm run audio:render-all` (requires GEMINI_TTS_API_KEY or ELEVENLABS_API_KEY env var). Until populated, the runtime falls back to Web Speech API as usual.";
const entries = {};
const manifest = {
  version,
  engine,
  renderedAt,
  notes,
  entries
};
export {
  manifest as default,
  engine,
  entries,
  notes,
  renderedAt,
  version
};
